@model WedstrijdCreateVM
@{
    ViewData["Title"] = "Create";
}
<script>
function myFunction() {
  // Declare variables
  var input, filter, ul, li, a, i, txtValue;
  input = document.getElementById('thuisClubID');
  filter = input.value.toUpperCase();
  ul = document.getElementById("myUL");
  li = ul.getElementsByTagName('li');

  // Loop through all list items, and hide those who don't match the search query
  for (i = 0; i < li.length; i++) {
    a = li[i].getElementsByTagName("a")[0];
    txtValue = a.textContent || a.innerText;
    if (txtValue.toUpperCase().indexOf(filter) > -1) {
      li[i].style.display = "";
    } else {
      li[i].style.display = "none";
    }
  }
}
</script>

<body>
    <input id="speelDatum" type="datetime-local" asp-for="speelDatum"
           class=" form-control" placeholder="Voer geboortedatum in"
           pattern="^(?:(?:31(\/|-|\.)(?:0?[13578]|1[02]))\1|(?:(?:29|30)(\/|-|\.)(?:0?[13-9]|1[0-2])\2))(?:(?:1[6-9]|[2-9]\d)?\d{2})$|^(?:29(\/|-|\.)0?2\3(?:(?:(?:1[6-9]|[2-9]\d)?(?:0[48]|[2468][048]|[13579][26])|(?:(?:16|[2468][048]|[3579][26])00))))$|^(?:0?[1-9]|1\d|2[0-8])(\/|-|\.)(?:(?:0?[1-9])|(?:1[0-2]))\4(?:(?:1[6-9]|[2-9]\d)?\d{2})$"
           required>
    <br>
    <div class="d-flex justify-content-between">
        <div>
            <select name="thuisClubID" id="thuisClubID">
                @foreach (ClubVM cvm in Model.Clubs)
                {
                    <option value=@cvm.ID>@cvm.Name</option>
                }


            </select>
            <select name="thuisTeamID" id="thuisTeamID">
                @foreach (TeamVM tvm in Model.Teams)
                {
                    <option value=@tvm.ID>@tvm.Name</option>
                }


            </select>

        </div>
        <div>
            <select name="uitClubID" id="uitClubID">
                @foreach (ClubVM cvm in Model.Clubs)
                {
                    <option value=@cvm.ID>@cvm.Name</option>
                }

            </select>
            <select name="uitTeamID" id="uitTeamID">
                @foreach (TeamVM tvm in Model.Teams)
                {
                    <option value=@tvm.ID>@tvm.Name</option>
                }

            </select>

        </div>
    </div>
</body>

